// Mrite v2.0 — 全局配置
const path = require('path');

module.exports = {
  projectsDirName: 'projects',
  windowWidth: 960,
  windowHeightCollapsed: 760,
  windowHeightExpanded: 760,
  maxTurns: 2000,
  modifyMaxTurns: 500,
  totalTimeoutMs: 12 * 60 * 60 * 1000,
  stuckTimeoutMs: 60 * 60 * 1000,
  bashToolTimeoutMs: 10 * 60 * 1000,
};
